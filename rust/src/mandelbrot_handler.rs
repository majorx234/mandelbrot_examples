use crate::calc;
use std::collections::VecDeque;

pub struct MandelbrotHandler {}

impl MandelbrotHandler {
    pub fn get_mandelbrot(&self) -> Option<Vec<Vec<u8>>> {
        None
    }
    pub fn zoom_in(&self, x: usize, y: usize) {}
    pub fn zoom_out(&self, x: usize, y: usize) {}
}
